# AGENTS.md — Sentinel EVC Lab

本文件是给 AI 编码助手（dsh / Claude Code / Codex / Cursor 等）的**工作指令**。
它位于 `.git` 所在层级（仓库根），会被自动加载。**动手改任何代码前，先把本文件读完。**

> 本机路径、当前分支和动态进度在 `SESSION_HANDOFF.md`。该文件不进版本库；
> 它不存在时，从实际仓库识别进度并重新建立记录。

## 本轮分批实施规则

以下规则由维护者在 2026-09-19 确认；与本文后面的历史环境或实现状态冲突时，
以本节和 `docs/spec-open-questions.md` 为准。

- 原方案决定业务语义；分批实施计划决定分支、阶段和验收；
  `karpathy-guidelines` 只指导工程风格，不覆盖项目合同。
- 当前工作按 `chore/00-repo-skeleton`、`feat/01-contracts-schemas`、
  `feat/02-core-evidence`、`feat/03-executor-presentation`、
  `test/04-integration-gates`、`release/05-v0.1-candidate` 顺序进行。
  每批测试、审查和提交完成后停止，未经维护者确认不合并、不推送、不进入下一批。
- 每个有独立意义的改动都要先运行相关测试、检查 diff，再单独提交。提交信息使用
  中文意图行，并用 `Constraint`、`Tested`、`Not-tested`、`Confidence`、
  `Scope-risk` 等 trailer 记录依据和验证范围。
- 只提交本次相关内容；不夹带已有工作区修改、私钥、本机路径或动态进度；不改写历史。
- 本机路径和动态进度只写 `SESSION_HANDOFF.md`。该文件仅本地保存，并同时由
  `.gitignore` 与 `.git/info/exclude` 排除。
- Python 基线为 3.13；运行依赖只允许 `numpy`、`cryptography`，测试依赖只允许
  `pytest`。不增加其他运行或测试依赖。
- 领域对象使用 frozen dataclass；保留规范 JSON、HMAC 与 Ed25519 密钥分离、
  Executor 单写者、单调时钟、有界事件队列和 LOG_GAP、独立 verify、revoke 锁内
  三操作等约束。精确合同和维护者补充见 `docs/spec-open-questions.md`。
- Q 项不得自行补全。未决规则不阻塞无关工作，但不得据此新增公开字段、错误码、
  阈值、事件或协议行为。
- 实现遵循最小改动：不提前抽象、不预留钩子、不做无关重构，不用 skip、xfail
  或 mock 核心逻辑冒充通过。

---

## 0. 先读这个

这是一个**数值参考实现**。它不是产品，不是功能安全认证，不是真机系统。
动手前必读：`README.md`（能力状态表）、`docs/表述纪律.md`。

**本仓库唯一要证明的主张：**

> 上游对动作做过变换之后，变换前的验证结论就不再成立；
> 而现在的部署链里，没有人在最终提交点重新检查这件事。

它有三个特征，任何改动都不要破坏它们：**可证伪**（构造几何场景就能跑出反例）、
**零硬件门槛**（陌生人 clone 下来五分钟能复现）、**不需要撒谎**（全程不说「我们能保证安全」）。

---

## 1. 环境（Windows 本机已就绪）

| 项 | 值 |
| --- | --- |
| Python | **3.13** |
| 运行时依赖 | **只有 `numpy`、`cryptography`** |
| 测试依赖 | `pytest` |
| git | 使用当前环境可用的 Git |
| 远端 | 以 `git remote -v` 的实际结果为准 |

### 依赖纪律（硬约束）

**运行时依赖只允许 `numpy` 和 `cryptography`。** 不引入 `torch`、不引入 `scipy`、
不引入任何 web 框架。

理由很实际：**陌生人 `pip install` 一次成功的概率，直接决定这个仓库有没有人用。**

`report.html` 由 Python 字符串模板 + 内联 SVG 生成。**不要引入 React、不要 CDN、
不要构建步骤。** 生成出来的文件双击就能看，断网也能看。

### 命令（PowerShell）

```powershell
.\.venv\Scripts\Activate.ps1
python -m pip install -e ".[test]"
python -m pytest -q
```

### 命令（Git Bash —— README 原文可原样照抄）

在 Git Bash 中使用对应虚拟环境入口：

```bash
source .venv/Scripts/activate
python -m pytest -q
```

### 运行产物

- 输出目录**必须是空目录**。不要覆盖随包的 `sample_run/`。
- `runs/` 已在 `.gitignore` 里，不要提交运行产物。

---

## 2. 远端参考实现的历史基线

下列数字来自同步前已有提交及随包样例，不是批次 00 的新验收结论。后续只有按本轮
冻结合同重新运行相应门禁后，才能把结果写成“本次实测”。

这些数字只能作为回归调查线索，不能覆盖本轮冻结合同或直接写进新验收结论。

### 单元测试

```
python -m pytest -q      →  24 passed
python run_tests.py      →  24 passed, 0 failed      （当时的备用运行器）
```

### `demo --cases 1000` 第一幕 · 几何判定

| 指标 | 期望值 |
| --- | --- |
| 构造案例总数 | 1000 |
| 实际违规的子轨迹 | 500 |
| 路径 a（只验父轨迹）错误放行 | **500 / 500** ← 这就是本项目要解决的问题 |
| 路径 b（最终全检）错误放行 | 0 / 500 |
| 路径 c（Δ-Cert + 必要全检）错误放行 | 0 / 500 |
| 路径 b 完整检查调用 | 1000 |
| 路径 c 完整检查调用 | 500 |
| 直接继承次数 | 500 |
| 安全子轨迹通过 | **500 / 500**（误拒 0） ← 负对照，用来堵「你们不就是一变就拒吗」 |
| 独立实现交叉验证分歧 | **0** |
| 父证书建立的完整检查 | 1000 |

### 第二幕 · 故障注入（七类，全部必须阻断）

| 故障 | 期望错误码 |
| --- | --- |
| late_action | `TRACKING_TUBE` |
| lease_expired | `LEASE_EXPIRED` |
| lease_replay | `LEASE_REPLAY` |
| scene_changed | `CONTEXT_CHANGED` |
| queue_rev_changed | `CONTEXT_CHANGED` |
| revoke_race | —（游标 3/3/3） |
| cancel_unconfirmed | `CANCEL_UNCONFIRMED` |

**撤销后旧代次新增提交 = 0** ← 这个数必须是 0，不是 0 就是严重回归。

### 第三幕 · 证据

| 指标 | 期望值 |
| --- | --- |
| 事件条数 | 2031 |
| 独立校验 | PASS（7 层全过） |

### `tamper` 四种篡改 —— 必须 4/4 失败，且**四种不同失败特征**

| 篡改方式 | 失败的校验层 |
| --- | --- |
| 改事件里 1 个字节 | `file_digest` · `hash_chain` · `tip_hash` |
| 删掉最后 3 行 | `file_digest` · `event_count` · `tip_hash` |
| 换一把公钥 | `signature` |
| 改 run_id | `run_id` |

> 四种失败给四种不同报错，比「四次都失败」有说服力得多。**不要把这四种归并成一种错误。**

---

## 3. 五项发布不变量（每条对应 `tests/` 里的一个测试 —— 不许删）

1. 未授权通道不能写驱动 —— **只有 `Executor` 能调 `controller.submit`**
2. 同一许可不能重复消费
3. 已提交的不可撤销前缀不能被改写
4. 撤销后不新增旧代次本地提交
5. 取消未确认不得恢复旧计划

---

## 4. 关键设计约束（改代码前必读）

| 约束 | 为什么 |
| --- | --- |
| 所有对象是 **frozen dataclass** | 证书引用的动作不能被事后改写 |
| 哈希用**规范化 JSON**（排序键 + 固定浮点格式 + UTF-8 无 BOM） | 跨机器复现的前提 |
| Authority 的 HMAC 密钥与 Evidence 的 Ed25519 密钥**必须分开** | 用途不同，共用密钥是设计错误 |
| 只有 Executor 能调 `controller.submit` | 单写者不变量，用测试强制 |
| **单调时钟**判断到期，UTC 只做审计标签 | 不同主机 monotonic 起点不同 |
| 事件队列**有上限**，满了显式丢弃并记 `LOG_GAP` | 不无限堆内存，缺口要可见 |
| `revoke` 拿锁只做三件事就立刻放开 | **网络等待绝不能占着撤销锁** |

### 两个最容易写错的地方

1. **余量要从父的剩余量扣，不是每次回到最初值。**
   余量 30 mm，第一次变化耗 8 mm → 剩 22；第二次耗 5 mm → **剩 17**，不是 30−5=25。
   链式继承时这个错误会一路累积成错误放行。
2. **继承失败的结论是「无法证明，去做完整检查」，不是「一定会碰撞」。**
   返回 `REJECTED` 之前必须真的跑完整检查。

### 几何检查

**必须整段算，不能只查端点。** 两个端点都在障碍外、中间穿过球心，是最典型的漏检，
也是这类系统最常见的实现 bug。`tests/` 里有专门的单元测试锁死它，不许绕过。

### schema 已在批次 01 冻结

`schemas/` 下三份（`event` / `scenario` / `verdict`）已按
`docs/spec-open-questions.md` 在批次 01 统一复核和冻结。后续不得由单个模块自行加字段。
事件类型固定 13 种，首版就全部定义，**不要后加**：

`PROPOSAL` `TRANSFORM` `CERTIFICATE` `PREPARE` `COMMIT` `DISPATCH` `CONTROLLER_ACK`
`OBSERVED` `REVOKE` `CANCEL_ACK` `BACKUP` `OUTCOME` `LOG_GAP`

---

## 5. 表述纪律（**最重要的一节，写文档/提交信息/答辩材料前必读**）

原文件：`docs/表述纪律.md`。要点：

| 想说的话 | 前置条件 |
| --- | --- |
| 「减少了 X% 完整检查调用」 | 必须**同时报告**父证书建立成本、失败回退成本、端到端墙钟时间。只数函数调用次数不算数 |
| 「接入了真实 VLA」 | 加载了实际检查点并跑了推理。**只跑通仿真环境不算** |
| 「实现了闭环」 | 门禁真的改变了发送的动作。**影子模式不算** |
| 「零违规」 | 必须跟一句「在本组 N 个构造案例、该约束族范围内」 |
| 「测试通过 N 项」 | N **只能是本仓库本轮的数字**。历史材料的 57 / 50 / 12 / 65 / 51 项**不能累加、不能并列** |
| 「实时」 | 指在指定硬件与任务下测量并管理动作有效期，**不对应硬实时或功能安全等级** |
| 「保证安全」 | **任何条件下都不说这句** |

### 关于那个「50%」

它指的是**验证阶段的完整检查调用次数**，**不是整机提速**。
父证书的建立成本、继承失败的回退成本都必须一并计入才能谈端到端收益。
**本仓库没有端到端耗时数据，因此不作任何提速主张。**

### 三类信息必须分开标注

- **来源记载**：历史文档描述的成果，本轮未独立复验
- **工程建议**：新增的范围、工时、角色和目标
- **本次实测**：随包代码、测试与运行记录能支撑的结论

任何一句对外表述，都要能说清它属于哪一类。

### 三句必须常说的话

1. 本项目里的「许可」「证书」「验证」都有具体适用范围，**不是功能安全认证、
   不是物理停止证明、不是事故责任判断**。
2. `dt=50 ms`、`K≤4`、tracking reserve 等数值都是**这个数值 profile 的配置**，
   不是任何机器人的安全速度或人体保护距离。
3. 「零次观测到失效」只说明这组测试没发现这类问题，**不等于任意场景零事故**。

---

## 6. 代码地图

```
src/sentinel_evc/
├── contracts.py       # 不可变对象 + 规范化哈希
├── geometry.py        # 线段-球精确距离、盒边界、完整检查
├── delta_cert.py      # 余量账本、继承判定、依赖失效
├── authority.py       # Lease 签发、HMAC、一次性消费
├── executor.py        # 单写者、逐步 dispatch、撤销屏障
├── sim_controller.py  # 有容量的模拟控制器 + ACK
├── events.py          # 事件定义、有界队列、JSONL 写出
├── evidence.py        # 哈希链、manifest、Ed25519 签名与校验
├── pipeline.py        # 端到端编排
├── report.py          # 生成静态 report.html（内联 SVG）
├── scenarios.py       # 场景与轨迹构造、批量实验
└── cli.py             # demo / verify / tamper
```

**校验器是独立实现** —— 只读文件、自己重算，**不 import 写入方的任何模块**。
这一点很重要：如果校验器复用了写入方的代码，那它证明不了什么。改 evidence 时不许破坏这条。

---

## 7. 提交与 PR 规范

- PR 必须附：**测试、输入输出样例、已知限制**。
- **所有涉及许可、撤销、时钟语义的改动，至少由另一位主力 review。**
- 每人同时进行的主任务不超过 2 个。
- **每周演示一次完整运行**，不接受「我这个模块写完了」作为进度汇报。
  （两人项目最常见的死法不是写不出代码，是两周后发现各自对「动作变换」「上下文」
  的理解根本不一样。每周跑完整链路是唯一能早期暴露这件事的办法。）
- 提交信息里引用的任何数字，都要能被 `RESULTS.md` 的格式追溯（数字 + 命令 + 输出文件路径）。
- **负结果要保留在仓库里，不要删掉。** 特别欢迎「指出本仓库某个结论不成立的证据」。

---

## 8. 明确不要做的事（本轮范围外）

**不做**：训练任何模型、接 openpi / LIBERO、装 MuJoCo、碰 ROS2、做 WorldGuard、
做前后端分离管理台、接真机、做闭环干预。

`worldguard/` 目录当前**只放 `README_WHY_EMPTY.md`**。原方案没有公开接口签名，
不要编造接口。
空目录配诚实说明，比塞一个没验证过的模型强。

`README.md` 顶部那张**能力状态表是项目最重要的诚信装置**。
任何时候它都必须与仓库实际状态一致 —— 实现了就改「已实现」，没实现就保持「未开始」，
**不要为了让 README 好看而提前勾上**。

---

## 9. 许可与公开发布（红线，务必遵守）

- 许可证文件现在是 `LICENSE.proposed`，**尚未生效**。
  在团队就代码作者、学校、指导教师、合作方的权利归属达成**书面共识**之前，
  **不要把它改名为 `LICENSE`**。
- **在相关技术披露顺序确认之前，不要把仓库转为 public。** 这可能影响新颖性。
- **发布前必须清查个人信息**（学号、电话、个人邮箱、身份证、住址），**包括 git 历史**。
  一旦推上去就撤不回来了。
- 商业计划书**一页都不进公开仓库**。
- 私钥不进仓库；demo 公钥要标注「**演示用，非客户 PKI**」。
- CI badge **必须在确认 CI 真的跑绿之后再挂**，不能预先挂。
