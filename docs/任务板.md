# 任务板

这张表是**仓库里的任务板**，与实施方案的排期和验收门禁（G0–G5）对齐。
每条任务后面跟的是能自己核对的证据（commit / CI 运行 / Issue）。

> **状态**：v0.1 已完成并打 tag；v0.2（影子模式）**未开始**，取决于第 0 步环境摸底。
>
> **GitHub 图形看板已建**：<https://github.com/users/DOU7326/projects/2>
> （16 项，含 Issue #1，状态列已填；负责人已加入可见范围）。
>
> ⚠️ 它**挂不到本仓库的 Projects 标签下** —— GitHub 的限制原话是
> 「Only projects owned by the same owner as the repository can be linked」：
> 仓库属 `LancerLSY`，看板属 `DOU7326`。若要挂进仓库，需要仓库 owner 在自己的
> 账号下建一个看板，内容照抄本文件即可。

图例：✅ 已完成 · ⬜ 未开始 · 🔴 卡在决策

---

## 一、v0.1 已完成（数值参考实现）

| # | 任务 | 状态 | 证据 |
| --- | --- | --- | --- |
| 1 | Demo 第一步（第一幕）：对照图 + 三条说明 | ✅ | `docs/demo_a.svg` · Issue #1 · commit `83e9eb0` |
| 2 | 第一幕在 CI 上自证（1000 案例 + 基线断言 + Artifact） | ✅ | commit `45a334b` · CI run #4 |
| 3 | README 中英双版（含三句必须常说的话、真实 VLA 接入状态） | ✅ | `README.md` · `README.zh-CN.md` · commit `ed401e5` |
| 4 | 对照图双语化（英文 README 不再嵌中文图） | ✅ | `docs/demo_a.en.svg` · commit `61b07bf` |
| 5 | 净室复现测验（全新 clone + 全新 venv 跑通全部命令） | ✅ | commit `4789362` · Issue #1 评论 |
| 6 | 修掉 `core.autocrlf` 导致的随包证据包校验失败 | ✅ | `.gitattributes` · commit `4789362` |
| 7 | MIT 许可证启用 | ✅ | `LICENSE` · GitHub 识别 SPDX: MIT · commit `3343624` |
| 8 | 依赖许可逐项登记 | ✅ | `docs/依赖许可.md` · commit `f02732a` |
| 9 | 个人信息清查（含 git 历史） | ✅ | 9 个提交全扫无命中 · Issue #1 评论 |
| 10 | v0.1.0 tag 与 Release | ✅ | [v0.1.0](https://github.com/LancerLSY/sentinel-evc-lab/releases/tag/v0.1.0) |
| 11 | `RESULTS.md` 与随包 `sample_run` 的自动对账 | ✅ | `tools/check_results.py` · CI 的 `demo-a` 作业 |

当前基线（可复跑）：`pytest -q` → 24 passed；`demo --cases 1000` → 路径 a 500/500、
b 0/500、c 0/500，误拒 0，交叉验证分歧 0；`verify` PASS（2031 条，7 层）；
`tamper` 4/4 失败且四种不同特征。

---

## 二、v0.1 收尾（等人或等决策）

| # | 任务 | 状态 | 卡在哪 / 谁来做 |
| --- | --- | --- | --- |
| 12 | G5 外部复现：**非项目成员**按 README 跑一遍 | ⬜ | 必须换一台没装过环境的机器，且由项目外的人执行。本轮的净室测验是项目内 AI 做的，**不算 G5** |
| 13 | 决策：专利「备选第二件」到底提不提 | 🔴 | 负责人 + 指导教师**书面确认**。它聚焦增量重验，与 `delta_cert.py` 高度重合 |
| 14 | 转 public | 🔴 | 依赖第 13 条；另需完成发布前检查清单剩余项 |
| 15 | 挂 CI 徽章 + 截 GitHub 真实封面 | ⬜ | 私有仓库里 badge 图片返回 404（实测），**转 public 后才能挂**。封面目前是本地渲染版 |
| 16 | 建立 GitHub Projects 看板 | ✅ | [看板](https://github.com/users/DOU7326/projects/2)（挂不进仓库 Projects 标签，原因见文首） |
| 17 | 五分钟演示录屏 | ⬜ | 实施方案里 C 角色的活，需要真人出镜 |

---

## 三、v0.2 影子模式（未开始，有硬时限）

目标不是闭环干预，而是拿到一句有实测支撑的话：

> 在真实上游链路里，策略输出的原始动作块和最终提交给环境的动作，确实不是同一个东西。

| # | 步骤 | 状态 | 关键点 |
| --- | --- | --- | --- |
| 18 | 步骤 0：环境摸底 | ⬜ | 能否按官方文档跑起 openpi 的 LIBERO 示例。**最多两周，装不上就放弃这条线** |
| 19 | 步骤 1：资产冻结 | ⬜ | 记到 `upstream_manifest.json`：仓库 commit、子模块 commit、检查点摘要、预处理、控制器配置、随机种子 |
| 20 | 步骤 2：原生复现 | ⬜ | 按官方默认配置跑 10 个固定初态存基线。**不用论文的成功率代替本机基线** |
| 21 | 步骤 3：双钩子（纯记录） | ⬜ | 前钩子记原始动作块；后钩子记最终提交动作。**只写文件，不判断、不改数值** |
| 22 | 步骤 4：逐步对齐 | ⬜ | 原生/透传逐步差异表。**查不清原因就不要往下走** |
| 23 | 步骤 5：离线判定 | ⬜ | 拿记录的最终动作跑本仓库检查器：会拒绝几次、为什么、差异分布 |

先按原生默认（当前示例 `replan_steps=5`）完成透传，**再**把 `K=4` 作为受控实验因素引入，
两者不能偷偷混同。详见 `docs/下一步_影子模式接入.md`。

---

## 四、绝不砍的四件事（来自实施方案）

动作语义定义 · 撤销测试 · 独立对照实验 · 能力状态表。
遇到压力时的砍单顺序（从下往上）：额外任务 → 页面美化 → 动态障碍 → 复杂优化 → 第二个场景族。
